# CMake out-of-source build trees + generated files
build/
build-*/
out/
_emu/          # installer clones + builds patched emulators here
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake

# Visual Studio / MSBuild
.vs/
*.user
*.suo
*.vcxproj.user
[Dd]ebug/
[Rr]elease/
x64/
Win32/
Libs/
# ...but the debugger backend source lives here and must be tracked.
!FrontEnd/src/Debug/
!FrontEnd/src/Debug/**

# Stray render / test output (harnesses write PNGs; none are tracked here)
*.png

# Runtime state the app writes at exit (per-user, not source)
assembly_comments.txt
saturn_explorer_watches.json
saturn_functions.txt          # Call Stack function-name renames
